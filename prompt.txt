You are a cricket data analyst. I am uploading a CSV file with IPL 2026 player statistics.

YOUR TASK: Add a column called "player_category" with values A, B, or C to every row. Return the full CSV as a downloadable file.

═══════════════════════════════════════════════════════
STEP 1: CLASSIFY EACH PLAYER BY ROLE
═══════════════════════════════════════════════════════

Read the "role" column. Classify using the rules for that role below.

── BATSMAN / WK-BATSMAN ──

  Category A (any ONE true):
    • bat_runs >= 3000
    • bat_100s >= 3
    • bat_average >= 35 AND bat_sr >= 130 AND bat_innings >= 50
    • bat_50s >= 20

  Category B (any ONE true, after A is excluded):
    • bat_runs >= 500
    • bat_average >= 25 AND bat_innings >= 20
    • bat_matches >= 50

  Category C:
    • Everyone else (few matches, low stats, or mostly blank data)

── BOWLER ──

  Category A (any ONE true):
    • bowl_wickets >= 80
    • bowl_avg <= 25 AND bowl_wickets >= 40
    • bowl_5w >= 1 AND bowl_wickets >= 50

  Category B (any ONE true, after A is excluded):
    • bowl_wickets >= 15
    • bowl_avg <= 35 AND bowl_innings >= 20
    • bowl_matches >= 30

  Category C:
    • Everyone else

── BATTING ALLROUNDER ──

  Category A (any ONE true):
    • bat_runs >= 2000
    • bat_runs >= 1000 AND bowl_wickets >= 20
    • bat_average >= 30 AND bat_sr >= 135 AND bat_innings >= 40

  Category B (any ONE true, after A is excluded):
    • bat_runs >= 500
    • bat_runs >= 300 AND bowl_wickets >= 5
    • bat_matches >= 30

  Category C:
    • Everyone else

── BOWLING ALLROUNDER ──

  Category A (any ONE true):
    • bowl_wickets >= 80
    • bowl_wickets >= 40 AND bat_runs >= 500
    • bowl_avg <= 25 AND bowl_wickets >= 30

  Category B (any ONE true, after A is excluded):
    • bowl_wickets >= 10
    • bat_runs >= 200 AND bowl_wickets >= 5
    • bowl_matches >= 20

  Category C:
    • Everyone else

═══════════════════════════════════════════════════════
STEP 2: BALANCE THE DISTRIBUTION
═══════════════════════════════════════════════════════

After Step 1, count players in A, B, C. The target is roughly equal (each 30-36% of total).

If A has too many players:
  - Move the WEAKEST A players (lowest runs or wickets) down to B.

If C has too many players:
  - Move the STRONGEST C players (highest runs or wickets among C) up to B.

Keep adjusting until each category has between 30% and 36% of total players.

DO NOT move obvious elite players (3000+ runs or 100+ wickets) out of A.
DO NOT move players with blank/missing stats out of C.

═══════════════════════════════════════════════════════
STEP 3: OUTPUT
═══════════════════════════════════════════════════════

1. Return the COMPLETE CSV with ALL original columns unchanged. Append "player_category" as the LAST column.

2. Provide it as a downloadable CSV file.

3. After the file, show this summary:

   a) Category distribution table (count + percentage for A, B, C)
   b) Top 3 players in each category with key stats
   c) Brief justification for any borderline decisions

═══════════════════════════════════════════════════════
IMPORTANT RULES — DO NOT VIOLATE
═══════════════════════════════════════════════════════

• Players with bat_runs >= 3000 MUST be Category A. No exceptions.
• Players with bowl_wickets >= 80 MUST be Category A. No exceptions.
• Players with ALL stats blank or zero MUST be Category C.
• Missing/blank values = insufficient data, lean toward C.
• Do NOT invent or modify any existing data values.
• Do NOT remove or reorder any columns.
